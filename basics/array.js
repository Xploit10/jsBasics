
let selectedColor = ['red', 'blue'];
selectedColor[2] = 1;
console.log(selectedColor, typeof(selectedColor), selectedColor.length);

// array is type object 
// array is a data structure
// Can store any size and any type

